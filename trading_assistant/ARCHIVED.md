# ⚠️ ARCHIVED — Do Not Develop

This directory is **not actively developed or maintained**.

It was built as a standalone experimental trading assistant application and
has not been reintegrated into the active V3 platform.  It remains here
for historical reference only.

## Active platform

The active runtime is:

```
src/spectraquant_v3/       ← V3 platform (crypto + equities)
src/spectraquant/          ← V2 platform (equities, legacy)
```

For V3 execution capabilities see `src/spectraquant_v3/execution/`.

## Do not

- Develop new features in this directory.
- Run `trading_assistant/` code against production data.
- Reference these paths in active V3 or V2 pipeline code.

## Archive copy

A parallel copy of this directory's README is preserved at
`archive/trading_assistant/README.md`.
